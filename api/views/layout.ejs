<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Email Template</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            height: 100vh;
            width: 100vw;
            background-color: #f4f4f4;
        }
        .email-container {
            width: 500px;
            max-width: 100%;
            background: #ffffff;
            border-radius: 8px;
            border: 1px solid rgb(223, 223, 223);
            display: flex;
            flex-direction: column;
            height: auto; /* Allow the container to adjust to content height */
            max-height: 80vh; /* Set a maximum height for the container */
        }
        .header {
            background: #007BFF;
            color: #ffffff;
            text-align: center;
            padding: 20px;
            font-size: 24px;
        }
        .image {
            text-align: center;
            height: 40vh;
            max-height: 40vh;
            overflow: hidden;
            display: flex;
            justify-content: center;
            align-items: center;
        }
        .image img {
            max-width: 100%;
            max-height: 100%;
            object-fit: cover;
        }
        .content {
            padding: 20px;
            color: #333333;
            font-size: inherit;
            line-height: 1.5;
            flex-grow: 1; /* Allow the content to grow */
            max-height: 30vh; /* Set a maximum height for the content */
            overflow-y: auto; /* Enable vertical scrolling if content exceeds max-height */
            overflow-x: hidden; /* Prevent horizontal overflow */
            word-wrap: break-word; /* Break long words to avoid horizontal scrolling */
            width: 100%; /* Ensure content fills the available width */
            box-sizing: border-box; /* Include padding and border in the element's width */
        }
        .footer {
            background: #f8f9fa;
            color: #6c757d;
            text-align: center;
            padding: 10px;
            font-size: 14px;
        }

        /* Responsive Design */
        @media (max-width: 768px) {
            .email-container {
                max-width: 100%;
                border-radius: 0;
                background:none
            }
            .header {
                font-size: 20px;
                padding: 15px;
            }
            .image {
                height: 30vh;
                max-height: 30vh;
            }
            .content {
                padding: 15px;
                font-size: 14px;
            }
            .footer {
                padding: 8px;
                font-size: 12px;
            }
        }
    </style>
</head>
<body>
    <div class="email-container">
        <div class="header">
            <%- title %>
        </div>

        <div class="image">
            <img src="<%= imageUrl %>" alt="Email Banner">
        </div>

        <div class="content">
            <%- content %> 
        </div>

        <div class="footer">
            <%= footer %>
        </div>
    </div>
</body>
</html>
